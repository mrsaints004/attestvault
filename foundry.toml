[profile.default]
src = "contracts/sol"
out = "out"
libs = ["node_modules"]
solc_version = "0.8.28"
optimizer = true
optimizer_runs = 200
via_ir = true

[rpc_endpoints]
creditcoin_testnet = "${CREDITCOIN_RPC_URL}"
source_chain = "${SOURCE_CHAIN_RPC_URL}"
